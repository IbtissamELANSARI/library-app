# Node.js dependencies
node_modules/

# Environment variables (Crucial for security!)
.env
*/.env # To ignore .env files in subdirectories (e.g., each service's .env)

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Build artifacts
dist/
build/
*.tgz

# OS-specific
.DS_Store
Thumbs.db

# Docker specific ignores (data volumes, etc.)
# Note: docker-compose.yml itself should be committed!
# If you have specific local docker data that shouldn't be versioned:
# .docker/
# .volumes/

# Example for specific service's build outputs if they exist outside standard
# menu-service/dist/
# commande-service/dist/